# Real contact data — lives in Firestore now, keep it out of the repo
contacts.json

# Local-dev only backend (app talks to Firestore directly in production)
server.py

# Background photos are no longer used (logo lives at repo root as logo.jpg)
images/

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# Python
__pycache__/
*.pyc
